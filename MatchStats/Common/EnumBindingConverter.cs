using System;
using ReactiveUI;

namespace MatchStats.Common
{
    //public class EnumBindingConverter : IBindingTypeConverter
    //{
    //    public int GetAffinityForObjects(Type lhs, Type rhs)
    //    {
    //        if (lhs == typeof (Enum) && rhs == typeof (string)) return 10;
    //        if (rhs == typeof (Enum) && lhs == typeof (string)) return 10;
    //        return 0;
    //    }

    //    public bool TryConvert(object from, Type toType, object conversionHint, out object result)
    //    {
    //        if (toType == typeof (Enum))
    //        {
                
    //        }
    //    }
    //}
}